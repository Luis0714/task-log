/** Clases del ítem del select de historias (estilos en globals.css → `.work-item-select-item`). */
export const WORK_ITEM_SELECT_ITEM_CLASS = "work-item-select-item";

/** Contenedor interno; usado por reglas de hover en modo oscuro. */
export const WORK_ITEM_SELECT_OPTION_CLASS = "work-item-select-option";
