export type ConnectPatFormValues = {
  organization: string;
  project: string;
  pat: string;
};

export const EMPTY_CONNECT_PAT_VALUES: ConnectPatFormValues = {
  organization: "",
  project: "",
  pat: "",
};
